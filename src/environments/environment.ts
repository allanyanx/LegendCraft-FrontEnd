export const environment = {
  production: true,
  // IMPORTANTE: Cuando tengas tu dominio, cambia esta URL.
  // Ejemplo: apiUrl: 'https://api.midominio.com/api',
  apiUrl: 'http://localhost:5104/api',
};
